import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DUMBSTAR-TRASH-APP',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
